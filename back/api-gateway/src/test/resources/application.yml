spring:
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false

logging:
  level:
    root: WARN

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false
