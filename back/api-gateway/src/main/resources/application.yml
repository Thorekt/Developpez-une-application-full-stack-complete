server:
  port: 9000

spring:
  application:
    name: api-gateway
  config:
    import: "configserver:http://localhost:9002/"
  cloud:
    config:
      fail-fast: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9001/eureka/
