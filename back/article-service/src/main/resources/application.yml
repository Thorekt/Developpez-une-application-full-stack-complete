server:
  port: 9300

spring:
  application:
    name: article-service
  config:
    import: "configserver:http://localhost:9002/"
  cloud:
    config:
      fail-fast: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9001/eureka/
