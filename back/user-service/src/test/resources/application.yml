spring:
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false

logging:
  level:
    root: WARN

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false

security:
  jwt:
    issuer: test-issuer
    expires-in: 3600
